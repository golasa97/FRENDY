#include "CommonUtils/VectorClearer.hpp"

using namespace frendy;

//constructor
VectorClearer::VectorClearer(void)
{
}

//destructor
VectorClearer::~VectorClearer(void)
{
}

//////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array2_int(vector<vector<Integer> >& vec)
{
  vector<vector<Integer> >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    vec[i].clear();
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array3_int(vector<vector<vector<Integer> > >& vec)
{
  vector<vector<vector<Integer> > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array2_int(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array4_int(vector<vector<vector<vector<Integer> > > >& vec)
{
  vector<vector<vector<vector<Integer> > > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array3_int(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array5_int(vector<vector<vector<vector<vector<Integer> > > > >& vec)
{
  vector<vector<vector<vector<vector<Integer> > > > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array4_int(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array6_int(vector<vector<vector<vector<vector<vector<Integer> > > > > >& vec)
{
  vector<vector<vector<vector<vector<vector<Integer> > > > > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array5_int(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array2_real(vector<vector<Real> >& vec)
{
  vector<vector<Real> >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    vec[i].clear();
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array3_real(vector<vector<vector<Real> > >& vec)
{
  vector<vector<vector<Real> > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array2_real(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array4_real(vector<vector<vector<vector<Real> > > >& vec)
{
  vector<vector<vector<vector<Real> > > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array3_real(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array5_real(vector<vector<vector<vector<vector<Real> > > > >& vec)
{
  vector<vector<vector<vector<vector<Real> > > > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array4_real(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array6_real(vector<vector<vector<vector<vector<vector<Real> > > > > >& vec)
{
  vector<vector<vector<vector<vector<vector<Real> > > > > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array5_real(vec[i]);
  //  }
  //  vec.clear();
  //}
}

//////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array2_int8(vector<vector<Integer8> >& vec)
{
  vector<vector<Integer8> >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    vec[i].clear();
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array3_int8(vector<vector<vector<Integer8> > >& vec)
{
  vector<vector<vector<Integer8> > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array2_int8(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array4_int8(vector<vector<vector<vector<Integer8> > > >& vec)
{
  vector<vector<vector<vector<Integer8> > > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array3_int8(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array5_int8(vector<vector<vector<vector<vector<Integer8> > > > >& vec)
{
  vector<vector<vector<vector<vector<Integer8> > > > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array4_int8(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array6_int8(vector<vector<vector<vector<vector<vector<Integer8> > > > > >& vec)
{
  vector<vector<vector<vector<vector<vector<Integer8> > > > > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array5_int8(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array2_real8(vector<vector<Real8> >& vec)
{
  vector<vector<Real8> >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    vec[i].clear();
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array3_real8(vector<vector<vector<Real8> > >& vec)
{
  vector<vector<vector<Real8> > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array2_real8(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array4_real8(vector<vector<vector<vector<Real8> > > >& vec)
{
  vector<vector<vector<vector<Real8> > > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array3_real8(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array5_real8(vector<vector<vector<vector<vector<Real8> > > > >& vec)
{
  vector<vector<vector<vector<vector<Real8> > > > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array4_real8(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array6_real8(vector<vector<vector<vector<vector<vector<Real8> > > > > >& vec)
{
  vector<vector<vector<vector<vector<vector<Real8> > > > > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array5_real8(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array1_str(vector<string>& vec)
{
  vector<string>().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<static_cast<int>(vec.size()); i++)
  //  {
  //    vec[i].clear();
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array2_str(vector<vector<string> >& vec)
{
  vector<vector<string> >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array1_str(vec[i]);
  //  }
  //  vec.clear();
  //}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

void VectorClearer::clear_vec_array3_str(vector<vector<vector<string> > >& vec)
{
  vector<vector<vector<string> > >().swap(vec);

  //int i_max = static_cast<int>(vec.size());
  //if( i_max > 0 )
  //{
  //  for(int i=0; i<i_max; i++)
  //  {
  //    clear_vec_array2_str(vec[i]);
  //  }
  //  vec.clear();
  //}
}

